server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/deeplr_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: jindun3d@
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
deeplr:
  datasource:
    initialization-mode: always 
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:5173/
  dataset:
    root-dir: D:/YueShaoPu/Web3D/png
