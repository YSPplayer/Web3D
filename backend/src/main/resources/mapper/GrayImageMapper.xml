<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deeplr.mapper.GrayImageMapper">

  <insert id="insertBatch" parameterType="java.util.List">
    INSERT INTO gray_image
      (img_key, img_path, img_value, width, height, interference_strength, img_status)
    VALUES
    <foreach collection="list" item="item" separator=",">
      (#{item.imgKey}, #{item.imgPath}, #{item.imgValue}, #{item.width}, #{item.height},
       #{item.interferenceStrength}, #{item.imgStatus})
    </foreach>
  </insert>

</mapper>
